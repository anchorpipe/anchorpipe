{
  "buildCommand": "cd apps/docs && npm install --legacy-peer-deps && (npm install --no-save --include=optional || npm install --no-save) && npm run build",
  "outputDirectory": "apps/docs/build",
  "framework": null,
  "installCommand": "npm install --legacy-peer-deps && (npm install --no-save --include=optional || npm install --no-save)",
  "rootDirectory": "apps/docs"
}
